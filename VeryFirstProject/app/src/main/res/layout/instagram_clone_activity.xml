<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context="ci.felight.ci.InstagramCloneActivity">

    <ImageView
        android:layout_width="wrap_content"
        android:layout_height="300sp"
        android:id="@+id/ivResult"
        android:src="@drawable/vidhya"
        android:layout_alignParentTop="true"
        android:layout_alignParentRight="true"
        android:layout_alignParentEnd="true" />

    <Button
        android:onClick="processImage"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="GreyScale"
        android:id="@+id/btngreyScale"
        android:layout_below="@+id/ivResult"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:layout_marginTop="51dp" />

    <Button
        android:onClick="processImage"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Blur"
        android:id="@+id/btnBlur"
        android:layout_marginRight="68dp"
        android:layout_marginEnd="68dp"
        android:layout_alignTop="@+id/btngreyScale"
        android:layout_alignRight="@+id/ivResult"
        android:layout_alignEnd="@+id/ivResult" />

    <Button
        android:onClick="getImageFromCamera"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Camera"
        android:id="@+id/btnCamera"
        android:layout_alignParentBottom="true"
        android:layout_alignRight="@+id/btngreyScale"
        android:layout_alignEnd="@+id/btngreyScale" />

</RelativeLayout>
